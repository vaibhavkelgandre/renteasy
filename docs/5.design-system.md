# 5. Design system

**Read this before writing any new markup.** Everything here exists so the tenth page needs no new
decisions.

Tailwind **v4**. Tokens live in `client/src/index.css`'s `@theme` block — **there is no
`tailwind.config.js` and no `postcss.config.js`**, which is the v4 way and will send you looking for
both if you follow a v3 tutorial.

---

## 1. Identity: teal on warm stone

| | Choice | Why |
|---|---|---|
| Neutral | **stone** | A *warm* grey. On a page where a stranger decides whether to lend you an ₹80,000 camera, warm neutrals read as friendly and cool ones read as administrative |
| Primary | **teal** (`--color-brand-*`) | Trust and availability, without emerald's "success message" connotation or blue's corporate-software feel — and distinct from the two colours that dominate this space, Airbnb's coral and every booking site's blue |
| Destructive | **rose**, only | The single red, so red always means "cannot be undone" |

`bg-stone-50`, not white: a page of white cards on white has no structure, and a faintly warm ground
gives every card an edge without borders everywhere.

## 2. Type

**System font stack, no webfonts.** A marketplace is found on a phone on mobile data, and a download
either blocks first paint or reflows visibly when it lands.

**16px base, deliberately not 14px.** An internal tool optimises for density because people live in
it; a consumer marketplace optimises for comfort because people arrive once and must not squint.

**`font-mono` + `.tabular` for prices and rental periods.** Money is the thing people compare here —
₹800/day against ₹15,000/month — and comparison needs digits that line up. Tabular figures make a
column of prices scannable instead of ragged.

## 3. Layout

**A light, translucent header — not a dark app bar.** A dark bar says "you are inside a system"; a
marketplace should feel like a shop window, with the chrome out of the way of the things being
rented.

**It renders for signed-out visitors too**, because browsing needs no account. That is why
`AppLayout` must not assume a user, and why `Header` and `VerifyBanner` each handle the absent case
themselves.

**`AppLayout` owns the page width** — no page sets its own, or the layout appears to shift as you
navigate.

**The width is `--container-content` in `index.css`, and it is 1440px.** It was `max-w-6xl`
(1152px), chosen so a grid of product cards would not sprawl. On a 1920px monitor that left about
380px of empty stone down each side while the cards it was protecting sat cramped three across —
protecting the layout from a problem it did not have, at the cost of one it did. 1440 is where the
large marketplaces land for the same reason: no dead margin on a laptop, still capped so a 2560px
display cannot stretch a row of cards into a horizon.

> **One token, because FOUR things need the same number**: the header, the verification banner,
> `AppLayout`'s `main`, and `Page`'s `full`. They were four hardcoded `max-w-6xl`s, so widening the
> content alone would have left the logo and the banner ending 150px short of it — a misalignment
> that reads as a rendering bug rather than as a width someone forgot to change. `--container-*` is
> the Tailwind v4 namespace behind `max-w-*`, so the token *is* the utility.
>
> **Verify a theme token by grepping the built CSS, not by reading the source.** A misspelled
> namespace produces no utility at all, and an unknown class in Tailwind is silently nothing — so
> the container would quietly become full-bleed rather than erroring. `max-w-content{max-width:
> var(--container-content)}` appearing in `dist` is the proof.

**The gutter grows with the screen** (`px-5` → `lg:px-8`). At 1440px of content a five-pixel margin
looks like the page is about to fall off the edge; on a phone anything more eats width the content
needs.

**Its vertical padding is asymmetric — `pt-5 pb-12`, not `py-8` — and that is deliberate.** Space
above the first thing on a page is paid for on every screen: it sat on top of a 64px sticky header
*and* the title block's own margin, around 215px of nothing before any content. Short pages
scrolled that had no business scrolling. Space at the BOTTOM costs nothing, because it is below the
last thing rather than above the first. The title block was tightened with it (`mb-5`, a `mb-2`
back link, `mt-1` under the heading), which is why both live in one place rather than per page.

> **The symptom to recognise:** a page whose content clearly fits, that still scrolls. Look at the
> accumulated chrome above it before looking at the content — four small margins in a column are
> worth more than any one of them looks.

> ⚠️ **That rule was written and then broken by every page.** Seven different widths accumulated —
> `max-w-md` through `max-w-6xl` — each page also re-applying the `px-5 py-8` AppLayout had already
> applied, so navigating visibly moved the content and changed the gutter.
>
> **`components/ui/Page.jsx` is now the only place either width is written**, and there are exactly
> two:
>
> | `width` | Value | For |
> |---|---|---|
> | `full` (default) | `max-w-6xl` | grids, lists, anything tabular |
> | `reading` | `max-w-2xl` | forms and prose — a text input at 1152px is unusable, and a paragraph that wide is hard to track back to the next line |
>
> **`Page` supplies width and nothing else** — no padding, since AppLayout already has it. Two
> widths is a decision; seven was an accident. Anything wanting a third changes that file, so the
> question gets asked once rather than per page.
>
> The standalone auth pages (sign in, register, verify, reset, check email) render **outside**
> AppLayout as centred cards, so their own narrow container is correct rather than a duplicate.

**`StatusBadge` lives in the same file, and covers every status in the product** — listings and
bookings alike. They were previously two hand-rolled maps in two files, which is how "Live" ends up
emerald on one screen and green on another. Tones are chosen by MEANING, so a reader learns the
palette once: stone is inert, emerald is good and current, amber wants attention, rose is over
unhappily, brand is in progress. `whitespace-nowrap` on the pill is load-bearing — a two-word label
in a narrow column otherwise wraps mid-pill and splits the rounded background into two stacked
halves.

**Browse puts its filters in a LEFT COLUMN, not a row across the top.** As a row, six controls plus
a date range wrapped onto two lines and pushed the first result off the screen — the page made you
scroll past the machinery to reach the thing you came for. A column costs width, which a 1152px
grid has, instead of height, which it does not. It is `lg:sticky` so narrowing a search does not
mean scrolling back up to change it, and every control shares one `CONTROL` class string: they sit
one under another in a 212px column, where any difference in height or radius between two of them
reads as a mistake rather than as variety.

**Tiles are 16:10, and the reason is a trap worth naming — it has now come up twice.** A card grid
in a container that gets wider does not automatically show more; it shows the same number, bigger.
And at a fixed aspect ratio a wider card is a **taller** card, so a wider page can show *fewer*
listings per screen than the narrow one did. Extra width has to be spent on **another column**
(`2xl:grid-cols-4`) or it works against you. First hit when the filters moved into a sidebar, again
when the container went to 1440px.

**The same trap, in its other form: a ratio alone means a wider page is a taller photograph.** The
listing gallery went 4:3 → 3:2 to stop it dominating the page, and the container widening would
have undone that on its own — 640px deep at 1440. It carries `max-h-[460px]` alongside the ratio,
so past the cap it crops instead of growing.

**A sidebar next to a fluid column should be FIXED, not a fraction.** `lg:grid-cols-[1.4fr_1fr]`
made the listing page's rate card grow with the window — 570px on a large monitor for a price, a
button and a calendar, none of which is better for being stretched. `minmax(0,1fr) 380px` hands
every extra pixel to the photograph and the description, which are the things a wider screen
actually helps. Same reasoning put a third field in the blackout form's row rather than letting two
date inputs share 1000px.

**A listing's availability calendar sits beside the price, not under the description.** "Is it free
that weekend?" and "what does it cost?" are one decision, and putting them in different halves of
the page meant scrolling past the whole description to answer half of it. Below the rate card
rather than above it, because the price is what brought the reader there.

## 4. The primitives

`client/src/components/ui/`. **Check here before writing markup** — the second hand-made button is
where drift starts.

| Component | Notes |
|---|---|
| `Button` | `variant`, `size`, `loading`, `fullWidth`, `as`. **`loading` implies disabled** — separating them is how a form gets submitted twice. `as={Link}` keeps a navigation a real link, so middle-click still works |
| `Field` | Bundles the label via `useId`. Marks what is **optional** rather than what is required — asterisks on every field of a signup form are noise, and people abandon forms that look longer than they are |
| `Checkbox` | `size-5`, not the browser's ~13px. The control carrying a legal agreement should not be the hardest thing on the page to hit |
| `Card` | Rounded generously, soft shadow — reads as a product tile, where a thin border reads as a data table |
| `Alert` | **Never a toast.** A toast disappears on a timer, which is exactly wrong for "check your inbox" — the reader looks away and the only instruction is gone |
| `Logo` | Inline SVG: an `<img>` costs a request and a flash of nothing |

**Sizes:** `md` is `h-12` (48px), taller than an internal tool's control. Used one-handed on a phone,
comfort beats compactness.

## 5. Rules that are easy to break by accident

**Copy that must stay true in several cases at once.** `CheckEmailPage` is the example: the API
answers identically for a new, unverified and verified address, so *"account created"* and *"we've
sent you a link"* are both **false in at least one case**. The wording has to be true in all three
and distinguish none — and there is a test asserting the rendered page contains none of the tempting
phrasings. It is genuinely easy to "improve" that copy into an enumeration oracle.

**A failure the API refuses to explain must not be explained here either.** `VerifyPage` shows one
message for expired, used, unknown and malformed tokens, because the API does. The copy is about
**what to do next**, which is the only useful thing to say when the cause is deliberately withheld.

**Nag, don't block.** `VerifyBanner` sits above the content for an unverified user. Blocking them
from browsing would hide the very thing that makes confirming worth doing; the refusal belongs at the
moment they try to list or book, where the reason is obvious.

**Mirror the server's single answer.** After a resend, the UI says "check your inbox" whatever
happened — sent, cooldown, already verified. Inventing a distinction the server refuses to make is
how the client becomes the leak.

**Status is words plus an indicator, never colour alone.**

**No mocked data.** `HomePage` has no fake listings: a placeholder tile reading "Canon EOS R6 —
₹800/day" is indistinguishable from a real one, and a page of invented inventory teaches you to
distrust the real listings when they arrive.

**A disabled control needs a reason.** The register button is disabled until the terms version loads
— the server refuses a stale version, so a button that could only fail is worse than one that waits.

## 6. Accessibility, as implemented

Not a checklist — the specific things the code does, so a new component can match:

- `Field` / `Checkbox` associate every label via `useId`, set `aria-invalid`, and link the message
  with `aria-describedby` so it is announced on focus.
- `Alert` uses `role="alert"` + `aria-live="assertive"` for errors, `role="status"` + `polite`
  otherwise.
- `Button` sets `aria-busy` while loading — the spinner is visual only.
- Loading states announce themselves in an `sr-only` `role="status"`; a pulsing logo tells a screen
  reader nothing.

**The tests assert these**, querying by label and by role — so a field that loses its label
association fails a test rather than shipping.

## 7. Deferred

**React Query** — declared in the stack, unused. It earns its place on data that is fetched, cached,
paginated and invalidated: listing grids, search results. A session is one object fetched once, and
`AuthContext` is less machinery for the same result. **Introduce it with the first real list** (step
5), not before.

**Dark mode** — not built. Would double every colour decision above. Revisit only if asked.
