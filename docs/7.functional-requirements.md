# 7. Functional requirements — the complete list

**Every requirement in the product, built or not.** This is the canonical list and the only one —
[`1.status.md`](1.status.md) points here rather than keeping its own copy, because two lists of
requirements always drift.

**Status:** ✅ built & tested · 🟡 partly built · ⚠️ code exists but **nothing uses it** · ⬜ not started

Last updated: **10 September 2026** · **149 requirements** — 61 built · 6 partial · 1 unwired · 81 not started

---

## How to use this

Cite the ID. `FR-103` means exactly one thing and will keep meaning it.

> *"Build FR-100 to FR-107 — listing create, rate card and publish."*
> *"FR-401's breakdown isn't itemised. Fix it."*
> *"What's blocking FR-500?"*

**IDs are never reused and never renumbered**, including if a requirement is dropped — a dead ID is
marked withdrawn and left in place, because a renumber silently invalidates every note and commit
message that referenced the old number.

Ranges are sparse on purpose, so a new requirement slots in beside its relatives instead of landing
at the end.

| Range | Area | Step |
|---|---|---|
| FR-001–039 | Accounts, authentication, profile | 1–2 |
| FR-100–139 | Listings and the rate card | 3 |
| FR-200–239 | Availability | 4 |
| FR-300–339 | Browse and search | 5 |
| FR-400–439 | Pricing and quotes | 6 |
| FR-500–539 | Bookings | 6 |
| FR-600–639 | Negotiation | 7 |
| FR-700–739 | Handover, return, completion | 8 |
| FR-800–839 | Reviews | 9 |
| FR-900–939 | Payments, deposits, payouts | 10 |
| FR-950–969 | Admin, moderation, disputes | later |
| FR-980–999 | Notifications | with each feature |

---

## Accounts & authentication — step 1 ✅

Spec: [`features/01-public-registration.md`](features/01-public-registration.md)

| ID | Requirement | Status |
|---|---|---|
| FR-001 | Registration is public — no invite, no code, no approval | ✅ |
| FR-002 | Name, email, password and explicit terms acceptance required; phone optional | ✅ |
| FR-003 | Email is the login identity, matched case-insensitively | ✅ |
| FR-004 | An account starts unverified | ✅ |
| FR-005 | Unverified users may sign in and browse, but **not** list, book or negotiate | 🟡 — gate written, **no endpoint uses it yet** |
| FR-006 | **The registration response never reveals whether an email is registered** | ✅ |
| FR-007 | Verification is a single-use token, valid 24h, stored only as a SHA-256 hash | ✅ |
| FR-008 | A token verifies an **email address**, not merely a user | ✅ |
| FR-009 | Verification can be resent, with a 2-minute cooldown | ✅ |
| FR-010 | The **version** of the terms accepted is recorded, with a timestamp | ✅ |
| FR-011 | Registration is rate limited by IP, counting successes | ✅ |
| FR-012 | Registration never sets a session cookie | ✅ |
| FR-013 | `is_admin` exists for platform staff and is never self-assignable | ✅ |

## Sign-in & sessions — step 2 ✅

| ID | Requirement | Status |
|---|---|---|
| FR-014 | Sign in with email and password; session in an httpOnly cookie | ✅ |
| FR-015 | Wrong password, unknown email and suspended account answer **identically** | ✅ |
| FR-016 | Sessions expire after 12 hours | ✅ |
| FR-017 | Sign out works whether or not a session exists | ✅ |
| FR-018 | `GET /auth/me` reads role and verification **fresh from the database** every request | ✅ |
| FR-019 | Sign-in is rate limited on **failed attempts only** | ✅ |

## Password reset — step 2 ✅

Spec: [`features/02-password-reset.md`](features/02-password-reset.md)

Hit as a real wall in development: an account's password was lost and the only recovery was deleting
the row.

| ID | Requirement | Status |
|---|---|---|
| FR-020 | Request a reset by email address | ✅ |
| FR-021 | **The response never reveals whether the address is registered** — same rule as FR-006 | ✅ |
| FR-022 | Single-use token, short expiry (1h), stored only as a hash | ✅ |
| FR-023 | Resend cooldown (15 min), returning silently when too soon | ✅ |
| FR-024 | A used, expired or unknown token answers **byte-identically** | ✅ |
| FR-025 | Setting a new password does not sign you in automatically | ✅ |
| FR-026 | The reset email carries no other credential and no session | ✅ |
| FR-027 | Sign-in page links to it | ✅ |

> **A reset does not sign out other sessions**, and that is the significant remaining gap in this
> area — a JWT is signed over the user id, not the password hash, so a session stolen before the
> reset stays valid for up to 12 hours after it. Needs a `token_version` column; tracked in
> [`1.status.md`](1.status.md) §8, not as an FR yet.
>
> **There is no "your password was changed" notification.** Standard practice, and the thing that
> tells a real owner their account was taken. Deliberately not built: nothing in FR-020–027 asks
> for it, and it should arrive as its own requirement rather than as a surprise.

## Profile — step 2 ✅

Spec: [`features/03-profile.md`](features/03-profile.md)

| ID | Requirement | Status |
|---|---|---|
| FR-028 | View your own name, email, phone, member-since and verification state | ✅ |
| FR-029 | Edit name | ✅ — phone too, see below |
| FR-030 | Change email — **re-verification required**, old address stays until confirmed | ✅ |
| FR-031 | Changing email invalidates outstanding verification tokens | ✅ |
| FR-032 | Change password, requiring the current one | ✅ |
| FR-033 | A public profile page: display name, member-since, rating, listing count — **never** email or phone | 🟡 — name, member-since and the never-rule done; **rating and listing count have no source yet** |
| FR-034 | Request account deletion | ✅ |

> **FR-033 is partial and will stay partial until steps 3 and 9.** The page, the route and the
> never-show-contact-details rule are all built and tested. Rating needs `reviews` and listing count
> needs `listings`; neither table exists. The API returns `null` for both — **"unknown", not zero** —
> and the page renders an em dash, because a confident `0` would read as "this person has never
> listed anything" rather than "listings do not exist".
>
> **FR-029 includes phone, which is one step beyond its literal wording.** Phone is captured at
> signup and was otherwise uncorrectable by anyone — a typo could never be fixed. It grants nothing:
> the number is unverified either way, and FR-036 gates high-value listings on a **verified** phone,
> which is a separate flow (FR-035) that does not exist.
>
> **FR-034 is a soft delete, and the consequence is user-visible: the address is not released.** The
> row survives (a marketplace's bookings and reviews reference a person, and the counterparty is
> entitled to their own history), so `uq_users_email_lower` still holds that address and the person
> cannot sign up with it again. There is a test pinning this as a known limitation rather than
> asserting it is desirable. Releasing the address needs a scrub step that is not built.

## Phone & trust tiers ⬜

| ID | Requirement | Status |
|---|---|---|
| FR-035 | Verify a phone number by OTP | ⬜ |
| FR-036 | Phone verification gates listings above a value threshold | ⬜ |
| FR-037 | ID / KYC verification as the highest tier | ⬜ |
| FR-038 | Trust tier shown on a public profile | ⬜ |

---

## Listings & rate card — step 3 ✅

Spec: [`features/04-listings.md`](features/04-listings.md) · Media: [`6.media-storage.md`](6.media-storage.md)

| ID | Requirement | Status |
|---|---|---|
| FR-100 | Create a listing as a **draft** — not visible to anyone else | ✅ |
| FR-101 | Required: title, description, category, condition | ✅ |
| FR-102 | Category from a fixed set (camera, bike, tool, appliance…) — a **lookup table**, not a CHECK | ✅ |
| FR-103 | **Rate card: hourly, daily and monthly prices — at least one required**, integer paise | ✅ |
| FR-104 | Security deposit amount, integer paise, may be zero | ✅ |
| FR-105 | 1–8 photos, 5MB each, real type sniffed, **EXIF stripped** | ✅ |
| FR-106 | Photo ordering; position 0 is the cover shown in the grid | ✅ |
| FR-107 | **Publish requires: a verified email, ≥1 photo, ≥1 rate, and a location** | ✅ |
| FR-108 | Edit any field while draft or published | ✅ |
| FR-109 | Unpublish — hides it from browse, keeps confirmed bookings intact | 🟡 — unpublish works; **the bookings half needs step 6** |
| FR-110 | Delete only when no active or upcoming booking exists | 🟡 — delete works; **the condition is not enforced, there is no table to consult** |
| FR-111 | **Only the owner may edit, unpublish or delete** — a relationship check, not a role | ✅ |
| FR-112 | **Changing the rate card never alters an already-confirmed booking** | ⬜ — belongs to bookings, see below |
| FR-113 | Location is an **area** (locality + city), never a street address | ✅ |
| FR-114 | Minimum and maximum rental duration per listing | ✅ — stored and validated; **enforced against a request at step 6** |
| FR-115 | Owner sees their own listings, drafts included | ✅ |
| FR-116 | Optional: pickup only, delivery available, or both | ✅ |

> **Three of these depend on a `bookings` table that arrives at step 6, and are marked honestly
> rather than ticked.**
>
> - **FR-112 cannot be built here at all.** "Changing the rate card never alters an already-confirmed
>   booking" is a guarantee a *booking* provides, by copying the agreed price into itself at the
>   moment it is confirmed. An owner editing a rate — which FR-108 explicitly permits — is precisely
>   the event FR-112 exists to survive, so refusing the edit would be the wrong fix.
> - **FR-110's condition is currently unenforced.** Deleting a listing is unconditional today. This is
>   the first thing to change when step 6 lands.
> - **FR-109's unpublish works**; whether it leaves confirmed bookings intact is untestable until
>   there are any.

## Availability — step 4 ⬜

| ID | Requirement | Status |
|---|---|---|
| FR-200 | Owner declares blackout dates | ⬜ |
| FR-201 | A confirmed booking blocks its dates automatically | ⬜ |
| FR-202 | **Two confirmed bookings can never overlap for one listing** — enforced by a database exclusion constraint, not an application check | ⬜ |
| FR-203 | Notice period: earliest a listing can be booked from | ⬜ |
| FR-204 | Owner sees a calendar of bookings and blackouts | ⬜ |
| FR-205 | Availability shown on the public listing page | ⬜ |
| FR-206 | Blackout dates cannot be added over an existing confirmed booking | ⬜ |

## Browse & search — step 5 ✅

Spec: [`features/05-browse.md`](features/05-browse.md)

| ID | Requirement | Status |
|---|---|---|
| FR-300 | Browse published listings **without an account** | ✅ |
| FR-301 | Filter by category | ✅ |
| FR-302 | Filter by price range, per chosen rental unit | ✅ |
| FR-303 | Filter by available dates | ⬜ — needs availability (step 4) |
| FR-304 | Filter by locality / city | ✅ — by city; locality is shown, not filtered on |
| FR-305 | Text search on title and description | ✅ — trigram, so partial words match |
| FR-306 | Sort by price, newest, rating | 🟡 — price and newest done; **rating needs reviews** (step 9) |
| FR-307 | Paginated, defaulted and capped; `total` counted with the **same WHERE** as the page | ✅ |
| FR-308 | Public listing detail page, no account needed | ✅ — built at step 3 |
| FR-309 | Unpublished, draft and deleted listings never appear | ✅ |
| FR-310 | A listing shows its owner's public profile and rating | 🟡 — links to the profile; **rating needs reviews** |

> **FR-307 is stronger than it asks for.** It requires `total` to be counted with the same WHERE as
> the page; the implementation takes it from the *same statement*, via `count(*) OVER ()`. Sharing a
> clause relies on discipline, sharing a statement makes disagreement impossible — and the failure
> it prevents is silent: a pager offering a page four that renders empty.
>
> **FR-304 filters by city, not locality.** Locality is displayed on every tile and detail page but
> is free text an owner types, so it has no canonical list to filter from — "Kothrud" and "Kothrud,
> Pune" would be different filters. City comes from the same free text and has the same weakness in
> principle, but a short derived list is usable in a dropdown where a long one is not. A real fix is
> a places lookup table, which is not worth it at this size.
>
> **FR-303 is the only one blocked**, and it needs step 4's blackout dates plus step 6's bookings to
> mean anything.

## Pricing & quotes — step 6 ✅ (bookings still to come)

The product's least obvious behaviour: [`0.product-overview.md`](0.product-overview.md) §4

| ID | Requirement | Status |
|---|---|---|
| FR-400 | **Charge the cheapest applicable combination of rates**, never a naive multiplication | ✅ |
| FR-401 | Show an **itemised breakdown** before booking: units, rate applied, subtotal | ✅ — API returns it; the UI lands with bookings |
| FR-402 | Deposit shown separately from rent, and labelled refundable | ✅ |
| FR-403 | Platform commission computed and recorded, not implied | ✅ — computed; **recorded** when `bookings` exists |
| FR-404 | **Quotes are computed server-side only.** A client-supplied total is never trusted | ✅ |
| FR-405 | A quote is recomputed at booking time; a stale one is refused | ⬜ — needs booking |
| FR-406 | Tax (GST) as a field from the first migration, even at zero | ✅ |
| FR-407 | Partial periods round in the renter's favour where ambiguous, and say so | ✅ |

> **The algorithm is exhaustive, not greedy, and that is the correctness argument.** Greedy — take
> as many months as fit, then days, then hours — gets the 40-day example right and the **6-hour one
> wrong**: six hours do not fill a day, so filling downward never considers buying one, and charges
> ₹900 where ₹800 was available. Rounding *up* to a larger unit is frequently the cheapest answer.
> The search space is tiny (durations are capped at a year), so exhaustive search costs microseconds
> and removes the need to reason about which rate relationships hold.
>
> **FR-407 in practice:** the duration rounds up to whole hours once, never per line, so it cannot
> compound — and since the combination chosen is always the cheapest, the rounding never costs the
> renter more than the unit they are being sold. A tie prefers fewer invoice lines, then larger
> units, so "1 month" beats "30 days" at the same money. That tie rule was **wrong on the first
> pass** and a test caught it.
>
> **Commission is taken from the owner's payout, not added to the renter's bill**, so the quoted
> price is the price paid. Nothing collects it — there is no gateway until step 10 — but the figure
> exists now because a commission introduced later would have to be back-computed for every
> historical booking.

## Bookings — step 6 ⬜

| ID | Requirement | Status |
|---|---|---|
| FR-500 | Request a booking for a date/time range | ⬜ |
| FR-501 | **Requires a verified email** — the first real consumer of FR-005 | ⬜ |
| FR-502 | **You cannot book your own listing** | ⬜ |
| FR-503 | Refused if it overlaps a confirmed booking or a blackout | ⬜ |
| FR-504 | Refused if outside the listing's min/max duration or notice period | ⬜ |
| FR-505 | States: `REQUESTED → ACCEPTED → ACTIVE → RETURNED → COMPLETED`, plus `DECLINED`, `CANCELLED`, `EXPIRED` | ⬜ |
| FR-506 | Illegal transitions refused server-side with `409`; the map lives in **one place** | ⬜ |
| FR-507 | Owner accepts or declines, with an optional message | ⬜ |
| FR-508 | A request not answered within N hours **expires automatically** | ⬜ |
| FR-509 | Renter may cancel before handover; policy determines the refund | ⬜ |
| FR-510 | Owner may cancel, and it counts against their reliability | ⬜ |
| FR-511 | **Every state change recorded with actor, timestamp and comment — append-only** | ⬜ |
| FR-512 | Both parties see the booking; **nobody else does** | ⬜ |
| FR-513 | Renter sees their bookings; owner sees bookings on their listings | ⬜ |
| FR-514 | **20 concurrent requests for the same dates produce exactly one confirmed booking** | ⬜ |

## Negotiation — step 7 ⬜

| ID | Requirement | Status |
|---|---|---|
| FR-600 | Renter proposes a different price on a booking request | ⬜ |
| FR-601 | Owner accepts, declines, or **counters** | ⬜ |
| FR-602 | Renter accepts or declines a counter | ⬜ |
| FR-603 | **Offers expire**, or an owner's inbox fills with stale offers on dates since booked | ⬜ |
| FR-604 | **An offer does not hold the dates.** Only a confirmed booking does — otherwise anyone blocks an item by offering ₹1 | ⬜ |
| FR-605 | **An accepted price is frozen into the booking.** The rate card may change; the booking cannot | ⬜ |
| FR-606 | Full offer history visible to both parties | ⬜ |
| FR-607 | Negotiation is per booking request, never per listing | ⬜ |
| FR-608 | Owner may disable negotiation on a listing | ⬜ |
| FR-609 | An offer below a floor the owner sets is auto-declined | ⬜ |

## Handover, return & completion — step 8 ⬜

| ID | Requirement | Status |
|---|---|---|
| FR-700 | Owner marks the item handed over; booking becomes `ACTIVE` | ⬜ |
| FR-701 | Renter confirms receipt | ⬜ |
| FR-702 | Condition photos at handover, by both parties | ⬜ |
| FR-703 | Renter marks the item returned | ⬜ |
| FR-704 | Owner confirms condition on return; booking becomes `COMPLETED` | ⬜ |
| FR-705 | Late return flagged, with a configurable late fee | ⬜ |
| FR-706 | Owner may raise a damage claim against the deposit | ⬜ |
| FR-707 | **Completion is what releases the deposit** | ⬜ |
| FR-708 | Neither party can complete unilaterally without a timeout | ⬜ |

## Reviews — step 9 ⬜

| ID | Requirement | Status |
|---|---|---|
| FR-800 | **Two-way** — owners review renters and renters review owners | ⬜ |
| FR-801 | **Only after a completed booking.** No booking, no review | ⬜ |
| FR-802 | One review per booking per direction | ⬜ |
| FR-803 | Rating 1–5 plus optional text | ⬜ |
| FR-804 | **Blind until both submit or the window closes** — otherwise the second review is a reply to the first | ⬜ |
| FR-805 | Editable for 48 hours, then immutable. Never deletable by its author | ⬜ |
| FR-806 | Aggregate rating on the public profile and on each listing | ⬜ |
| FR-807 | Reviews publicly readable | ⬜ |
| FR-808 | The subject may post one public reply | ⬜ |

## Payments, deposits & payouts — step 10 ⬜

| ID | Requirement | Status |
|---|---|---|
| FR-900 | Rent, refundable deposit and platform commission as separate amounts | ⬜ |
| FR-901 | **Every amount integer paise.** A float in a money path is a bug | ⬜ |
| FR-902 | **Never marked paid because the frontend said so** — only on server-side verification of the provider's webhook | ⬜ |
| FR-903 | Deposit **held** at booking and **returned** on completion — a different lifecycle from rent | ⬜ |
| FR-904 | Owner payout after completion, not at booking | ⬜ |
| FR-905 | Refunds follow a stated cancellation policy | ⬜ |
| FR-906 | A receipt per booking, itemised | ⬜ |
| FR-907 | Deposit deductions require an agreed or adjudicated damage claim | ⬜ |
| FR-908 | Every money movement is an **append-only ledger entry**; balances are derived, never stored | ⬜ |

## Admin, moderation & disputes ⬜

| ID | Requirement | Status |
|---|---|---|
| FR-950 | Admin can suspend a user; suspension ends their session on the next request | ⚠️ — `requireAdmin` exists, **no endpoint** |
| FR-951 | Admin can take down a listing, with a recorded reason | ⬜ |
| FR-952 | **Anyone can report a listing or a user** — a public marketplace will receive content that should not be there | ⬜ |
| FR-953 | Report queue for admins | ⬜ |
| FR-954 | Either party can raise a dispute on a booking | ⬜ |
| FR-955 | Admin adjudicates a dispute and directs the deposit | ⬜ |
| FR-956 | **Admin actions are audit-logged with the actor** | ⬜ |
| FR-957 | Admin can never grant themselves admin | ⬜ |

## Notifications ⬜

Delivered alongside the feature that triggers them, not as a separate step.

| ID | Requirement | Status |
|---|---|---|
| FR-980 | Email on booking requested, to the owner | ⬜ |
| FR-981 | Email on accepted / declined, to the renter | ⬜ |
| FR-982 | Email on a new offer or counter-offer | ⬜ |
| FR-983 | Reminder before handover and before return | ⬜ |
| FR-984 | Email on completion, with the receipt | ⬜ |
| FR-985 | **A notification failure never fails the action that triggered it** | ⬜ |
| FR-986 | In-app notification list with an unread count | ⬜ |
| FR-987 | **Never a figure or an address in a subject line** — it shows on a lock screen | ⬜ |

---

## Withdrawn

None yet. When a requirement is dropped it is listed here with its ID and the reason, and the ID is
never reissued.

## Deliberately not requirements

Named so their absence is a decision rather than an oversight.

| Not doing | Why |
|---|---|
| Property and apartment rentals | Needs rental agreements, longer terms and local legal compliance that would swamp everything else. V1 is portable goods |
| In-app chat | Negotiation and booking messages cover the real need. Free-text chat brings moderation and abuse handling with it |
| Delivery and logistics | Pickup only in V1 |
| Insurance | The deposit is the protection mechanism |
| Social sign-in | Email is enough, and each provider is its own integration |
| Multi-currency | Rupees only |
| Real-time anything | Nothing here changes on a sub-minute timescale |
